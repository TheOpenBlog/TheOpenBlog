language: php

php:
  - 5.6

before_script:
  - php artisan cache:clear
  - cp .env.travis .env
  - mysql -e 'create database OpenBlog_testing;'
  - composer self-update
  - composer install --no-interaction
  - php artisan key:generate

script:
  - vendor/bin/phpunit
